services:
  php:
    image: tugboatqa/php:apache
    default: true
    commands:
      init:
        - ln -snf "${TUGBOAT_ROOT}/public" "${DOCROOT}"
  mysql:
    image: tugboatqa/mysql:5.7.29
    commands:
      update:
        - php artisan migrate
        - php artisan db:seed
